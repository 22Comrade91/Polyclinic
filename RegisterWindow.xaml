<Window x:Class="HospitalApp.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация" Height="300" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="111*"/>
            <ColumnDefinition Width="289*"/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Width="300" Grid.ColumnSpan="2" Margin="50,0,0,0">
            <TextBlock Text="Имя пользователя" Margin="0,0,0,3" Width="250"/>
            <TextBox x:Name="UsernameTextBox" Width="250" Margin="0,0,0,5"/>

            <TextBlock Text="Пароль" Margin="0,0,0,3" Width="250"/>
            <PasswordBox x:Name="PasswordBox" Width="250" Margin="0,0,0,5"/>

            <TextBlock Text="Подтвердите пароль" Margin="0,0,0,3" Width="250"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Width="250" Margin="0,0,0,30"/>

            <Button Content="Зарегистрироваться" Width="250" Click="RegisterButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
